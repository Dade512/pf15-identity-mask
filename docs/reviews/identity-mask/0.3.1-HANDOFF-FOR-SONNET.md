# 0.3.1 Hygiene Pass — Handoff for Sonnet (from Fable, 2026-06-12)

Scope: execute the GitHub review's disposition list as **v0.3.1**. Do NOT move the v0.3.0 tag.
Do NOT begin 0.4.0. All work stays inside `S:\Campaign Material\Modules\pf15-identity-mask`.
Follow `docs/specs/identity-mask/ROADMAP.md` release discipline: runtime-affecting changes need
a two-client check before tag (Vivaldi=Dade GM, Chrome=DXWarlock player, world
`baphomets-fall-dev`, server http://192.168.56.1:30001; verify GM identity after every reload).

## Task list (in order)

### 1. PAT / remote hygiene (MICHAEL FIRST, then Sonnet)
- Michael: revoke the exposed PAT on GitHub; create replacement auth (gh CLI login or SSH).
- Sonnet: `git remote set-url origin https://github.com/Dade512/pf15-identity-mask.git`
  (no embedded credentials); verify push works via gh/SSH/credential manager.
  KNOWN ISSUE: pushes hang when Git Credential Manager pops a hidden GUI prompt; if gh CLI is
  authenticated this should resolve; otherwise test interactively with Michael present.
- Remove the PAT operational note from WORK-IN-PROGRESS.md (and from
  memory file `feedback_pf15im_roadmap_decisions.md` → mark rotated).

### 2. Code hardening (two small changes, then runtime smoke-test)
- `scripts/state.mjs` onChange: split tracker / chat / canvas sync into separate
  try/catch-guarded helpers; inside the chat sweep, wrap each `updateMessage` per-message so
  one failure cannot abort the rest or the nameplate refresh.
- `scripts/chat.mjs`: preserve third-party child markup in `.message-sender` — replace only
  TEXT NODES with the alias (e.g. walk childNodes, swap the text content of text nodes whose
  trimmed content matches/non-empty, or set only the first text node and blank the rest), so
  icons/links added by other modules survive. Keep textContent-only writes (no HTML).
- Runtime smoke: masked token IC message + reveal/conceal retroactive sweep on both clients;
  confirm sender masked, no console errors. (Pattern: see §15 acceptance + 0.3.0 census doc.)

### 3. 0.3.0 independent closeout report (missing artifact)
- Write `docs/reviews/identity-mask/2026-06-12-identity-mask-0.3.0-verify.md` in the same
  format as the 0.2.0 report (§ structure incl. independent closeout verdict). Evidence
  sources: census doc, spec §15, WIP notes, commit a39cc8f narrative, Michael's manual
  attack-card confirmation (goblin attacks + tracker showed "Shadowy Figure" on player seat),
  and your own §2 smoke-test results. Label honestly what was observed vs inherited.

### 4. Documentation de-drift
- `README.md`: remove "chat out of scope" contradiction; limitations section retitled for
  current version; future list = 0.4.0 onward only.
- `docs/specs/identity-mask/identity-mask.spec.md`: title/status → "v0.3.0 implemented /
  0.4.0 next target"; §5 visibility matrix gains nameplate + chat-sender rows (or pointers to
  §14/§15); §12 deferred features drop HUD/nameplate/chat; keep §14/§15 as-is.
- `WORK-IN-PROGRESS.md`: rewrite as a clean current-status doc (no PAT note, no local paths,
  no usernames/world ids/registry keys, no contradictory leftover sections). Transient detail
  lives in session history, not the release tree.

### 5. Release packaging
- `module.json`: bump 0.3.1; change download to a version-pinned URL —
  `https://github.com/Dade512/pf15-identity-mask/archive/refs/tags/v0.3.1.zip`
  (note: tag must exist before the URL resolves — acceptable; or create a GitHub Release with
  a built asset if Michael prefers; ask him once).
- Clean-install test: after tagging, download that ZIP, install into a scratch data folder or
  verify the wrapper-directory layout (GitHub zips wrap in `pf15-identity-mask-0.3.1/`) —
  confirm Foundry's installer handles it or document that manual install needs the inner
  folder. Record the result in the closeout report addendum.

### 6. Ship
- Validate: node --check all scripts, JSON valid, guardrail grep (innerHTML/eval/alert/
  console.log/prototype/jQuery/fetch), CSS scope check.
- Commit "v0.3.1: repository hygiene + sync hardening + sender-markup compatibility",
  tag v0.3.1, push main + tag — ONLY after Michael's explicit go.

## Context Sonnet needs
- Architecture + evidence: spec §3/§14/§15; reports for 0.1.0/0.2.0; census doc.
- Registry currently holds Michael's live test entry ("Shadowy Figure") — do not clear it.
- Dev-world anomaly (round-0 combat `Xv3MFaCSWsVSDpI0`, two Unknown Participants) is
  Michael's to review; don't touch.
- Player join: select user DXWarlock, no password, submit via `form.requestSubmit()` if
  coordinate clicks fail (ninja-notes overlay eats clicks; backgrounded tabs pause canvas
  ticker — known automation artifacts, see 0.2.0 report §5).
